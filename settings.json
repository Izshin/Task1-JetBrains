{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Sync settings",
    "description": "example of a boolean flag being used to enable or disable concurrent edit prevention",
    "properties": {
        "ConcurrentEdit": {
            "type": "boolean"
        }
    }
}